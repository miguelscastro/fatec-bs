import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#C7E1F8',
    paddingVertical: 15,
    alignItems: 'center',
    borderRadius: 4,
    borderWidth: 1,
  },
  text: {
    fontSize: 18,
    fontWeight: 'bold',
  },
});
