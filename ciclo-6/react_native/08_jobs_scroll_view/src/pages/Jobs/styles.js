import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  pageContainer: {
    flex: 1,
    paddingTop: 30,
  },
  title: {
    fontSize: 22,
    color: '#CB3030',
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 10,
  },
});
